import styled from 'styled-components';

export const Wrapper = styled.div`
    height: 100%;
    padding: 100px 50px;

    .header{
        max-height: 300px;
        object-fit: cover;
    }

    .customBtn{
        width: 150px;
        color: white;
        background-color: var(--darkColor);
    }
`;
